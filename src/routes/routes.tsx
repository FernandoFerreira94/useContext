import { Route, Routes, BrowserRouter } from "react-router-dom";

export default function RoutesApp() {
  <BrowserRouter>
    <Routes>
      <Route path="/" element={} />
    </Routes>
  </BrowserRouter>;
}
